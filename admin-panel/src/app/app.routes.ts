import { Routes } from '@angular/router';
import { ProductsComponent } from './machine test/products/products.component';

export const routes: Routes = [
    {
        path:'products',
        component:ProductsComponent
    }
];
